---
description: 開 PR 前自己審一遍 diff
argument-hint: "[base]"
---
讀 `git diff ${1:-HEAD}` 的完整內容,像 reviewer 一樣審一遍。測試綠燈不算審過。

特別找測試抓不到的那幾類:

- 改了行為但沒改對應的文件或註解
- 錯誤處理的分支從沒被執行過
- 新增的設定項沒有預設值,或預設值跟文件寫的不一樣
- 只在特定平台/特定順序下才會踩到的假設
- 為了讓測試過而放寬的判斷式
- 順手改掉、但跟這次目的無關的東西

逐項列出來,標上檔案和行號。沒問題的話直接說沒問題,不要湊數。
